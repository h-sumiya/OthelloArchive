// data [int;64] Null: 0 Black: 1 White: 2

export function legal_moves(data, color) {
    return;// [move]
}

export function flip(data, color, position) {
    return;// new data
}

//  date display
var     self.dires = [-10, -9, -8, -1, 1, 8, 9, 10]
        self.discs = ' - o x\n'
        self.bamed = [0 if i % 9 else 3 for i in range(91)]
        self.bamed[40] = self.bamed[50] = self.turn = 1
        self.bamed[41] = self.bamed[49] = 2
